# SONGLIM ERP 전수조사 보고서

- **작성일**: 2026-08-03
- **검증 방식**: 로컬 실행(backend `:8010` + frontend `:3000`) + Playwright 자동 스크린샷(웹 1440px / 모바일 390px) + 소스코드 정적 검수
- **스크린샷 커버리지**: 주요 17개 페이지 × 2뷰포트 = **34장** 캡처, 전 페이지 HTTP 200 / 콘솔 에러 0건

---

## 1. 이번 작업 반영 내역 (요청 3건)

| # | 요청 | 반영 위치 | 검증 |
|---|---|---|---|
| 1 | 영업노트 본인 작성분 수정/삭제 | 백엔드 `PATCH/DELETE /api/sales-notes/{id}` (작성자 본인만), 프론트 영업노트 탭 + 영업지도 병원 상세 패널 | PATCH/DELETE 200, 타인 노트 편집 시 **404**, 미인증 시 **401** 확인 |
| 2 | 클라우드 NAS → **자료실** | 사이드바 / 페이지 헤더 / breadcrumb / 메타 title | 스크린샷 확인 |
| 3 | 소모품 카탈로그 관리 → **소모품 관리** | 사이드바 / 페이지 헤더 / breadcrumb / 관리자페이지 안내문구 | 스크린샷 확인 |

- 빌드 검증: `tsc --noEmit` 통과(에러 0), 백엔드 앱 정상 import.
- 영업노트 권한 경계는 개인메모(`personal_memos`)와 동일한 "작성자 본인만" 패턴을 그대로 따름 — 전 직원이 **열람**은 공유하되 **수정/삭제는 작성자 본인만** 가능.

---

## 2. 스크린샷 전수조사 결과

### 정상 확인된 항목
- **전 페이지 렌더링 정상** — 데스크톱/모바일 모두 200, JS 콘솔 에러 0건.
- **다크모드/라이트모드** — 이전에 수정한 자료실 "올리기" 버튼(라이트모드 가시성) 정상.
- **모바일 반응형** — 대시보드/영업지도/자료실 등 카드·리스트형 화면은 모바일에서 잘 접힘(사이드바 햄버거, 필터 세로 스택).
- **네이버 지도** — 영업지도 200개 병원 마커·클러스터·범례 정상.

### 발견된 UI/UX 이슈

| 심각도 | 위치 | 증상 | 원인 | 제안 |
|---|---|---|---|---|
| **중** | 소모품 관리 카탈로그 표 (모바일) | 표 헤더(제조사/카테고리/단위)가 글자 단위로 세로로 쪼개져 깨짐 | `<table className="w-full">`에 `min-w`가 없어 `overflow-x-auto`가 작동하지 않고 컬럼이 찌그러짐 ([AdminCatalogClient.tsx:114](frontend/src/components/supply/AdminCatalogClient.tsx#L114)) | 표에 `min-w-[720px]` 부여 → 좌우 스크롤로 전환 |
| **중** | 소모품 관리 "카테고리별 노출 제한" 폼 (모바일) | 입력창+셀렉트+"허용 추가" 버튼이 한 줄에 몰려 버튼이 화면 밖으로 밀림 | flex 행이 모바일에서 wrap되지 않음 | `flex-wrap` 추가 |
| **낮** | 전역 | 한글이 OS 기본 폰트(맑은고딕/애플고딕/Noto)로 제각각 렌더링 | 폰트가 `Outfit`(라틴 전용)만 로드됨 → 한글은 폴백 ([layout.tsx:10](frontend/src/app/layout.tsx#L10)) | 아래 3장 폰트 항목 참고 |

---

## 3. 폰트 점검 (요청 명시 항목)

- 현재 `next/font/google`의 **Outfit**을 `subsets: ["latin"]`으로만 로드 → **한글 글리프가 없음**. UI의 95%가 한글인데 실제로는 브라우저/OS 기본 산세리프로 폴백되어 **기기마다 자간·굵기·모양이 달라짐**(Windows=맑은고딕, macOS=Apple SD Gothic, Android=Noto).
- **권장**: 한글 웹폰트(**Pretendard** 또는 **Noto Sans KR**)를 `next/font`(local 또는 google)로 추가하고 body 기본 폰트 체인에 포함. Outfit은 숫자/영문 표기에만 보조로 유지 가능.
- **부가 발견**: 루트 `<html lang="en">`인데 콘텐츠는 전부 한국어 ([layout.tsx:34](frontend/src/app/layout.tsx#L34)). 스크린리더·검색엔진·브라우저 번역이 오작동할 수 있음 → `lang="ko"`로 변경 권장(1줄).

---

## 4. 코드 품질 점검

### 4-1. 날짜 KST 오프바이원 버그 (프로젝트 CLAUDE.md가 금지한 안티패턴)
`new Date().toISOString().slice(0, 10)`은 한국(UTC+9)에서 자정~오전 9시 사이에 **날짜가 하루 전으로 밀린다.** 현재 7곳에 존재:

- [Calendar.tsx:30](frontend/src/components/calendar/Calendar.tsx#L30)
- [mileage/page.tsx:27,34,36](frontend/src/app/(admin)/mileage/page.tsx#L27) (운행일지 기본 날짜/월 시작/내보내기 종료일)
- [StaffDashboard.tsx:102](frontend/src/components/dashboard/StaffDashboard.tsx#L102) ("오늘 일정" 판정)
- [HospitalDetailPanel.tsx:31](frontend/src/components/sales-map/HospitalDetailPanel.tsx#L31) (영업노트 방문일 기본값)
- [AdminOrdersClient.tsx:28](frontend/src/components/supply/AdminOrdersClient.tsx#L28)

→ `getFullYear()/getMonth()/getDate()` 기반 로컬 날짜 헬퍼로 통일 권장(공통 util 1개 추출).
> ⚠️ 이번 작업 범위(요청 3건)에는 포함하지 않고 **문서에만 기록**했습니다. 수정 원하시면 별도 지시 주세요.

### 4-2. 테스트 커버리지
- 백엔드: **5개**(회원가입 승인/로그인 성공·실패/브루트포스 잠금/발주). 핵심 인증·권한만 커버.
- 프론트: **자동 테스트 없음**.
- 이번에 추가한 영업노트 수정/삭제 및 권한 경계는 자동 테스트 미포함 → `test_sales_note_permissions` 추가 권장(작성자 본인만 수정/삭제, 타인 404).

### 4-3. 기타
- TODO/FIXME/HACK 마커 **0건** — 코드 위생 양호.
- 라우터↔모델 1:1 구조, 도메인 분리 명확.

---

## 5. 보안 점검 결과

| 항목 | 상태 | 비고 |
|---|---|---|
| 비밀번호 해싱 | ✅ 양호 | bcrypt(`security.py`) |
| 브루트포스 방어 | ✅ 양호 | 5회 실패 → 15분 잠금 |
| CORS | ✅ 양호 | 환경변수 화이트리스트(기본 localhost), 와일드카드 아님, `allow_credentials=True` |
| 세션 쿠키 | ✅ 양호 | HttpOnly, SameSite=Lax, `COOKIE_SECURE` 환경변수 |
| 권한 이중화 | ✅ 양호 | 사내 페이지 `require_staff`+`StaffOnly`, 관리자 `require_admin`+`adminOnly` |
| 영업노트 수정/삭제 권한 | ✅ **이번 검증** | 타인 노트 404, 미인증 401 |
| 감사 로그 | ✅ 존재 | 백그라운드 기록(관리자 UI 미노출) |
| **HTTPS** | ⚠️ 미적용 | 도메인 확정 대기(IP+HTTP 운영). 확정 시 `COOKIE_SECURE=true` 필수 |
| **관리자 2FA** | ⚠️ 없음 | 관리자 계정 탈취 시 전체 노출 — OTP 도입 검토 |
| 업로드 파일 | 참고 | 이미지 리사이즈/WebP 변환은 있으나 확장자·MIME 화이트리스트 재확인 권장 |

---

## 6. 타 ERP·영업지도 대비 추가 제안 기능

### 영업/CRM 강화
1. **영업노트 F/U 리마인더** — 노트에 "다음 방문 예정일"을 달면 캘린더/알림벨로 자동 리마인드(현재는 단순 기록만).
2. **영업 파이프라인(딜 단계)** — 병원별 리드→상담→견적→계약 단계 관리. 계약 데이터와 연결하면 전환율/영업 실적 대시보드 산출 가능.
3. **담당자별 실적 대시보드** — 계약 금액·건수·활동량(노트/방문) 리포트. 현재 대시보드는 KPI 카드만 존재.

### 영업지도 강화
4. **장비 노후도 히트맵** — 보유장비 `year` 기반으로 "교체 임박(7~10년 경과)" 병원을 색으로 강조 → 교체 영업 타겟팅. 데이터(`equipment.year`)는 이미 있음.
5. **방문 동선 최적화** — 선택한 병원들 간 최적 방문 경로(외판원 문제) 제안. 운행일지와 연계.
6. **경쟁장비 필터 프리셋 저장** — 자주 쓰는 필터 조합 저장(시도+제조사+연식).

### 운영/생산성
7. **전역 통합검색** — 병원/계약/납품/영업노트를 한 검색창에서.
8. **발주 승인 워크플로우** — 금액 임계 초과 발주는 관리자 승인 단계.
9. **재고 부족 알림** — `inv_supplies` 임계치 이하 시 알림벨/이메일.
10. **알림 채널 확장** — 계약/발주 상태 변경을 카카오워크·슬랙·이메일로 푸시(현재 알림벨 내부만).
11. **관리자용 감사 로그 뷰어** — 이미 기록 중인 `audit_logs`를 관리자 화면에서 조회.

### 모바일/현장
12. **PWA 오프라인 캐시** — 매니페스트·서비스워커는 이미 있음. 현장(병원)에서 오프라인 열람/작성 후 동기화 지원 시 활용도↑.
13. **명함/장비 라벨 촬영 인식** — 계약서 OCR(클로바) 인프라를 재활용해 현장 장비 시리얼/모델 촬영 등록.

---

## 7. 우선순위 제안 (권장 처리 순서)

**즉시(빠른 수정, 저위험)**
- `<html lang="ko">` 변경 (1줄)
- 한글 웹폰트(Pretendard/Noto Sans KR) 도입
- 소모품 관리 표 `min-w` + 노출제한 폼 `flex-wrap` (모바일 깨짐 2건)

**단기**
- KST 날짜 헬퍼 공통화 + 7곳 치환
- 영업노트 권한 pytest 추가
- HTTPS 전환 시 `COOKIE_SECURE=true`

**중장기(기획 필요)**
- 영업 파이프라인 / F/U 리마인더 / 담당자 실적 대시보드
- 장비 노후도 히트맵
- 알림 채널(카카오/슬랙/이메일) 연동

---

> 본 보고서의 "발견 사항"은 대부분 정보 제공 목적이며, 이번 커밋에는 **요청하신 3건(영업노트 수정/삭제, 자료실·소모품 관리 리네이밍)만** 반영했습니다. 위 개선 항목 중 진행할 대상을 지정해 주시면 이어서 작업하겠습니다.
