# 송림 ERP 작업 정리 (2026-09-18)

New.md 2077줄 이후 요청 전체 + 그 앞의 자료실/OCR 작업까지 한 번에 정리한 문서입니다.
맨 아래 **"사장님이 판단하셔야 할 것"** 절에 결정이 필요한 항목만 따로 모아 두었습니다.

---

## 0. 지금 상태 요약

| 구분 | 상태 |
|---|---|
| 배포된 것 | 자료실 폴더 업로드 + 계약서 OCR (커밋 `01794d7`, `1f55ab9`) — **운영 반영 완료** |
| 아직 커밋 안 된 것 | 이번 문서의 1~11번 전부 (로컬에만 있음) |
| 백엔드 테스트 | 16/16 통과 |
| 프론트 | `tsc --noEmit` 통과, `next build` 통과 (38개 라우트) |
| 로컬 확인 | 아래 "로컬 확인 방법" 참고 |

---

## 1. 자료실 / 사내문서서식 (배포 완료)

**틀이 계속 길어지던 문제** — 파일이 늘수록 카드가 세로로 무한정 늘어났습니다.
화면 높이에 고정(`h-[calc(100vh-11.5rem)]`)하고 목록 영역만 스크롤되게 바꿨습니다.

**파일 1개를 드래그하면 `.` 폴더가 생기던 버그** — 원인은 react-dropzone이 쓰는
`file-selector` 라이브러리가 **파일 하나만 드롭했을 때 경로를 `"./파일명"`으로 채우는 것**이었습니다.
그 경로를 `/`로 잘라 폴더를 만들다 보니 `.` 폴더가 생겼습니다. 해당 라이브러리를 이 화면에서
걷어내고 자체 탐색기(`dropUpload.ts`)로 교체했습니다.

**폴더 통째로 업로드** — 윈도우 탐색기와 동일하게 하위 폴더 구조 전체가 그대로 재현됩니다.
- 폴더 안의 폴더, 그 안의 폴더까지 재귀 탐색
- **파일이 없는 빈 폴더도 그대로 생성**
- 브라우저가 한 번에 100개씩만 알려주는 특성 때문에 **폴더에 파일이 100개 넘으면 누락되던 문제**도 해결
- 폴더 타일 위에 떨어뜨리면 그 폴더 안으로 업로드
- 같은 폴더를 다시 올려도 동명 폴더가 늘지 않고 병합

**한계**: `↑ 폴더 올리기` 버튼은 브라우저가 빈 폴더를 알려주지 않아 빈 폴더가 재현되지 않습니다.
드래그&드롭으로 올리면 빈 폴더까지 그대로 옵니다.

---

## 2. 계약서 OCR (배포 완료)

실제 계약서 사진을 클로바 OCR에 넣어 응답(123개 필드)을 받아 그 실데이터로 고쳤습니다.

**고치기 전 상태**: 전화번호 칸에 `"휴 대 폰 팩 스"`가 들어가고, 주소·상품·기타·계좌가 통째로 누락.

**원인 2가지**
1. 양식 라벨이 `주 소`, `휴 대 폰`처럼 자간이 벌어져 OCR이 **한 글자씩 끊어** 줍니다.
   → 연속 토큰을 이어붙여 라벨을 찾도록 수정 (`주`+`소`=`주소`)
2. 판매금액·기타·상품표는 값이 라벨 오른쪽이 아니라 **아래 줄**에 있습니다.
   → 헤더 라벨의 x좌표로 컬럼 경계를 만들어 아래 줄을 칸별로 분배

**결과 — 계약서 15개 칸 전부 일치**
계약일자 / 병원명 / 사업자번호 / 대표자 / 주소 / 품목 2건+수량+비고 / 판매금액+VAT /
기타 / 설치희망일(손글씨 `2026.5.19`도 인식) / 고객지불계좌 / 예금주.
빈 칸 4개(전화·휴대폰·팩스·고객요구사항)는 빈 값으로 유지됩니다.

**부수 작업**: 고객지불계좌·예금주 필드를 새로 만들어 등록폼·상세화면까지 연결.
회귀 테스트 3건 추가(픽스처의 고객 정보는 가상값으로 익명화 — 실제 고객 정보를 저장소에 남기지 않기 위함).

---

## 3. 왼쪽 메뉴 재구성

지시하신 순서 그대로입니다.

1. **캘린더**
2. **계약 및 재고** (기존 그대로)
3. **송림 커뮤니티** — 회사 공지사항 / 입찰정보 / 사내 문서 서식
4. **병원 커뮤니티** — 병원 공지사항 / CS / 의료소식 / 공동구매 / 중고기기
5. **자료실** (기존 "사내 정보" 대분류 삭제, 그 자리에 단일 항목)
6. **소모품 발주 내역**
7. **영업 DATA** — 영업지도 / 장비별 판매 통계 / 의료기관 장비 검색 / 의료기관 개설 현황 / 의료기관 분포현황

"병원지도 → 영업지도", "영업지도 통계 → 장비별 판매 통계" 개명 완료.

**커뮤니티 페이지는 지시대로 삭제**했습니다. 삭제 전에 확인해서 사고를 막은 게 하나 있는데,
`tech-posts` API가 커뮤니티 전용이 아니라 **공동구매·중고기기와 공유**되고 있었습니다.
다만 그 둘은 상세 페이지 경로를 쓰지 않아 영향이 없었고, **백엔드 API는 그대로 뒀습니다.**
알림함이 삭제된 `/community`로 링크를 걸고 있던 것도 함께 정리했습니다.

---

## 4. 날짜 처리

확인 결과 **이미 정리돼 있었습니다.** 남아 있던 `.toISOString()` 1곳은 `Date.UTC` 기반이라
의도적으로 올바른 코드였습니다. 납품 화면의 중복 헬퍼만 공용 `lib/date.ts`로 통일했습니다.

---

## 5. 입찰정보 첨부파일

공고명을 클릭하면 **그 공고의 첨부파일 전체가 한 번에** 펼쳐집니다(입찰공고서·규격서 등).
파일별로 확장자 배지 + 파일명 + 새 탭 열기. 첨부가 없는 공고는 "첨부파일 없음"으로 명확히 표시.

데이터는 이미 수집돼 있었고(41개 공고, 114개 파일) 화면에 안 나오고 있던 것이었습니다.
기존 공고명 클릭 동작(원문 링크)은 별도 "원문 보기" 열로 보존했습니다.

---

## 6. 재고관리 (초음파 / 장비) 스프레드시트화

**편집**: 모든 칸이 상시 편집 가능, 자동 저장. Tab·Enter·화살표 이동, **Shift+화살표 범위 선택**,
**Ctrl+C/X/V로 엑셀·구글시트와 직접 복사·붙여넣기(TSV)**, Ctrl+Z 실행취소, 헤더 클릭 정렬,
열별 필터, 고정 헤더, 행 추가/삭제.

**동시 수정 충돌** — 행이 아니라 **셀 단위로 비교**합니다. 두 사람이 같은 행의 다른 칸을 고치는 건
정상 통과시키고, **같은 칸이 부딪힐 때만** "내 값 / 현재 값(먼저 고친 사람)"을 띄워 고르게 합니다.
조용히 덮어써지는 일이 없습니다.

**변경 이력** — 누가 언제 어느 칸을 무엇에서 무엇으로 바꿨는지 전용 테이블에 기록.
행 이름·작성자 이름을 스냅샷으로 남겨 행이나 계정이 지워져도 이력이 읽힙니다.
다른 사람이 바꾼 칸은 3초간 노란 하이라이트 + "○○님이 방금 수정".

**실시간 방식** — WebSocket이 아니라 **3초 델타 폴링**입니다. 변경분만 내려주므로 아무도 안 고치면
트래픽이 0에 수렴하고, 탭이 백그라운드면 멈춥니다. WebSocket이 기술적으로 불가한 게 아니라,
브라우저↔서버 핸드셰이크를 이 환경에서 실검증할 방법이 없어 **배포 구성 변경이 0인 쪽**을 먼저 택했습니다.
나중에 WebSocket으로 올릴 때 프론트 로직은 그대로 재사용됩니다.

---

## 7. 영업지도 전수조사

사장님이 느끼신 "병원이 합쳐진다"의 **진짜 원인은 이름 중복이 아니었습니다.**

- **이름 중복은 문제가 아니었음** — 이름이 같은 480그룹/1,834행을 전부 검사했는데 **전원 요양기호가 달랐고**,
  이미 개별 병원으로 올바르게 저장돼 있었습니다. 강남베드로/성남베드로도 정상적으로 구분됩니다.
- **진짜 원인 ①: 시군구 표기가 두 벌로 갈라져 있었음.** 심평원 데이터는 `부산남구`, 동물병원 데이터는 `남구`.
  드롭다운에 둘 다 뜨고 어느 쪽을 골라도 나머지가 검색되지 않았습니다.
  → **4,417행 표기 통일 + 1,067행 시→구 승격**. 시군구 종류 370 → 285개로 정리.
- **진짜 원인 ②: 29,100행(47.7%)이 같은 좌표를 공유.** 메디컬빌딩이라 대부분 정상인데,
  마커가 완전히 겹쳐 **1개로 보이고 맨 위 한 곳만 클릭**됐습니다.
  → 마커 그룹화 + 개수 배지 + 클릭 시 그 좌표의 병원 목록 펼치기.
- **좌표 오류** — 주소와 50km 이상 어긋난 305행을 지도에서 제외(목록 검색에는 그대로 나옵니다).
  `자윤한의원` 서울 양천구 → 제주시 451km 같은 사례가 있었고, **좌표가 서로 맞바뀐 쌍**이 다수라
  임포트 계통 오류로 보입니다.
- 검색 목록에 시도·시군구·주소를 항상 표시해 같은 이름도 구분됩니다.
- "의원" 검색처럼 결과가 5만 건인 경우 500건만 나오는데 안내가 없었습니다 → 경고 배너 추가.

**행을 지우거나 병합하는 파괴적 변경은 하지 않았습니다.** 총 행수 64,713 불변.

**광주 → 전라남도 통합** (지시 반영)
- 심평원 유래 1,814행의 시도를 광주광역시 → 전라남도로
- 전남 안에 무접두로 남아 있던 광주 자치구 130행을 `광주북구` 형태로 통일
- 결과: 광주광역시 잔여 0행, 전라남도 4,002행, 총 행수 불변

---

## 8. 의료기관 데이터 3개 화면 (mediharu 참고)

`/med/equipment-search`, `/med/openings`, `/med/distribution` 신설.

**장비 검색** — 장비 종류·제조사·모델·지역·연도로 검색하면 보유 의료기관이 나오고,
병원명으로 검색하면 그 병원의 보유 장비 전체가 나옵니다. **전 카테고리 완전 커버**:
초음파 227K / X-ray 149K / 골밀도 70K / C-Arm 37K / CT 15K / MRI 12K, 제조사 102종, 모델 2,951종.

**분포 현황** — 시도·시군구별 분포를 표와 지도 버블로. 지도는 기존 영업지도와 같은 방식.

**성능** — 장비 51만 행, 병원 6.5만 행이라 전부 서버에서 집계하고 페이지네이션합니다.
인덱스를 추가해 개설 추이 쿼리가 501ms → 86ms로 빨라졌습니다.

**부수 발견** — 병원 분류에 **동물병원 5,457곳이 섞여** 있어 분리했습니다(병원 7,441 → 1,984).

---

## 9. 심평원 개설일자 연동 ★ 이번에 가장 큰 수확

받아두신 API 키(기존 `HIRA_API_KEY`, 디코딩키)로 **병원정보서비스**를 연동했습니다.
이 API가 개설일자뿐 아니라 **주소와 좌표까지** 같이 내려준다는 걸 확인해서, 제가 "가장 근본적인
공백"이라고 보고했던 문제들을 한 번에 메웠습니다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 개설일자 | **0행** | **55,145행** |
| 주소 누락 | 59,256행 (91.6%) | **4,111행** |
| 좌표 누락 | 4,031행 | 3,749행 |

실측 확인: 2026년 796건 / 2025년 2,027 / 2024년 2,194 / 최근 30일 6건.
이제 개설 현황이 **연 단위 추정이 아니라 실제 날짜 기준 일·월 단위**로 동작합니다.

개설 현황 화면도 실제 날짜 기반으로 교체했습니다 — 기간 프리셋(7일/30일/90일/1년/3년/5년/전체)이
실제로 동작하고, 추이가 연 단위에서 **월 단위**로 바뀌었으며, 신규 목록에 실제 개설일자와 주소가 나옵니다.

⚠️ **꼭 알아두실 데이터 특성 — 심평원 신고 지연**
최근 4~5개월치는 아직 집계가 덜 반영돼 있습니다. 실측하면 이렇습니다:

| 월 | 건수 | |
|---|---|---|
| 2026-03 | 195건 | 정상 수준 |
| 2026-04 | 196건 | 정상 수준 |
| 2026-05 | 75건 | 지연 시작 |
| 2026-06 | 7건 | |
| 2026-07 | 6건 | |
| 2026-08 | 10건 | |
| 2026-09 | 2건 | |

즉 **"최근 30일 6건"은 실제 개설 수가 아니라 아직 집계가 안 된 수치**입니다(실제로는 월 200건 안팎).
오해를 막기 위해 7일·30일·90일 구간을 고르면 화면에 경고 배너가 뜨도록 했습니다.
**추세 비교에는 1년 이상 구간을 쓰시는 게 정확합니다.**

**여전히 불가능한 것**: 심평원 병원정보서비스에는 **폐업일자가 없습니다.**
폐업·휴업 집계는 이 API로는 만들 수 없습니다. 화면에도 "제공하지 않음"으로 명시해 두었습니다.

---

## 10. 카카오톡 단톡방 알림

**먼저 사실관계**: 일반 카카오톡 단체 채팅방에 메시지를 넣는 **공식 API는 2026년 현재도 없습니다.**
카카오 공식 개발자 포럼에서 카카오 직원이 직접 확인해 준 내용입니다.

선택하신 **PC 카톡 UI 자동화** 방식으로 구현했습니다.

```
발주 등록 / 공유일정 등록·변경
   → 서버가 알림을 대기열에 적재
   → 사내 PC의 브리지가 20초마다 서버에서 가져감
   → 카카오톡 PC 창을 제어해 단톡방에 전송
   → 결과를 서버에 보고 (실패 시 5회까지 재시도)
```

서버가 사내 PC로 접속하는 게 아니라 **PC가 서버로 가지러 오는** 구조라 사내망에 포트를 열 필요가 없습니다.

- 파일: `tools/kakao_bridge/` (`bridge.py`, `run_bridge.bat`, `requirements.txt`)
- 서버 `.env`에 `KAKAO_BRIDGE_TOKEN` 설정 필요. **비워두면 기능이 통째로 꺼집니다**(안전 기본값)
- `KAKAO_DRY_RUN=1`로 **실제 전송 없이 내용만 찍어볼 수 있습니다** — 먼저 이걸로 테스트 권장
- 알림 생성과 전송 수단을 분리해서, 나중에 사내알림/메일로 바꿔도 발주·캘린더 코드는 안 건드려도 됩니다
- 검증 완료: 토큰 차단, 줄바꿈 보존, 실패 재시도, 성공 시 큐 제거 (테스트 4건)

⚠️ **이 방식의 위험** — 카카오 약관은 자동화 도구 사용을 금지합니다. 계정 제재 가능성이 있고,
카톡이 업데이트되면 깨질 수 있습니다. **업무 전용 계정으로 돌리시고 개인 계정은 쓰지 마세요.**

---

## 11. 디자인 전면 수정

선택하신 **"데이터 밀도형 엔터프라이즈"** 방향으로 디자인 시스템을 새로 만들고 전 화면에 적용했습니다.

**원칙**
- 강조색을 쓰지 않고 **회색 계조로만 위계**를 만듭니다
- 주요 액션 버튼도 브랜드 파랑이 아니라 **중성 진회색**, 화면당 하나만
- 컨트롤 높이 36px로 통일, 표는 촘촘하게
- 색은 **의미가 있을 때만** — 상태 점, 삭제 버튼, 경고 배너

**적용 범위**: 공용 레이아웃, 계약, 납품, 소모품 발주(6개 화면), 자료실, 영업지도(3개 패널),
재고, 의료기관 통계, 게시판류, 대시보드, 폼 요소 전체.

**색 면적을 줄이면서 바뀐 것** — 재고 관리의 카테고리 헤더 색칠이 중성으로 바뀌었습니다.
지멘스/타사 구분은 상단 4px 컬러 바 + 선택 링 + 범위 배경으로 남아 있습니다.

---

## 로컬 확인 방법

지금 두 서버가 떠 있습니다.

| | 주소 |
|---|---|
| **사이트** | **http://127.0.0.1:3001** |
| API | http://127.0.0.1:8010 |

**임시 로그인 계정** (확인용으로 제가 만든 것 — 확인 끝나면 지우시거나 말씀해 주세요)
```
아이디   qa_local_tmp
비밀번호 local1234
```

**둘러보실 만한 곳**
- http://127.0.0.1:3001/inventory/equipment — 스프레드시트 재고 (셀 편집, Ctrl+C/V, 변경 이력)
- http://127.0.0.1:3001/med/equipment-search — 의료기관 장비 검색
- http://127.0.0.1:3001/med/openings — 개설 현황 (실제 개설일자)
- http://127.0.0.1:3001/med/distribution — 분포 현황
- http://127.0.0.1:3001/sales-map — 영업지도 (마커 그룹화, 시군구 정리 결과)
- http://127.0.0.1:3001/bids — 입찰정보 (공고명 클릭 → 첨부파일)
- http://127.0.0.1:3001/storage — 자료실 (폴더 드래그&드롭)
- http://127.0.0.1:3001/contracts/new — 계약서 OCR

서버를 끄시려면 터미널에서 해당 프로세스를 종료하시면 됩니다.

---

# ★ 사장님이 판단하셔야 할 것

## A. 지금 바로 결정이 필요한 것

### A-1. 배포 여부
1~11번 전부 **아직 커밋·배포하지 않았습니다.** 로컬에만 있습니다.
로컬에서 확인해 보시고 "배포해줘" 하시면 진행합니다.

배포 시 **운영 DB에서 따로 실행해야 하는 작업**이 있습니다(코드 배포만으로는 반영 안 됨):
```
backend/scripts/normalize_hospital_region.py   --apply   # 시군구 표기 통일
backend/scripts/clear_bad_hospital_coords.py   --apply   # 좌표 오류 제거
backend/scripts/merge_gwangju_into_jeonnam.py  --apply   # 광주 → 전남
backend/scripts/sync_hira_hospital_info.py     --apply   # 개설일자·주소·좌표 (약 2분)
```
넷 다 실행 전 백업 자동 생성 + 되돌리기 파일 생성 + 재실행해도 안전(멱등)합니다.

### A-2. 카카오톡 단톡방 이름
`tools/kakao_bridge/run_bridge.bat`에 채워야 동작합니다. **정확한 채팅방 이름**을 알려주세요.
그리고 **먼저 `KAKAO_DRY_RUN=1`로 테스트**해 보시길 강하게 권합니다.
(약관 위반 소지가 있는 방식이라, 업무 전용 계정 사용을 권합니다)

### A-3. 재고 관리의 카테고리 색 구분
디자인 원칙(색 면적 최소화)에 따라 **카테고리 헤더 색칠을 중성으로** 바꿨습니다.
지멘스/타사 구분이 헤더 색이 핵심이었다면 체감이 달라집니다.
써보시고 구분이 약하면 **되돌릴 수 있습니다.**

---

## B. 데이터 판단 — ✅ 전부 처리 완료 (2026-09-18, 지시대로 임의 판단하여 적용)

`backend/scripts/fix_hospital_data_issues.py` (dry-run 기본 + 백업 + undo + 멱등)

### B-1. 요양기호 중복 3쌍 → 병합 완료
3쌍 모두 **같은 요양기호·같은 시군구·같은 개설일**이라 동일 기관의 이중 등록이 확실했습니다.
- `(사)`, `(스카이메디)` 같은 접두어가 붙은 쪽은 표시명으로 나쁘므로 **깨끗한 이름 쪽을 남겼습니다.**
- 없어지는 쪽의 장비 **40건은 남는 쪽으로 이전**했습니다. 통째로 옮기면 같은 장비가 두 번 잡혀
  보유 대수가 부풀기 때문에 (분류·연도·모델·제조사) 기준으로 중복을 걸렀습니다(실제 중복 0건).
- 결과: 병원 64,713 → **64,710행**, 요양기호 중복 0, 고아 장비 0.

### B-2. 인천 신설구 73행 → 접두어 통일
서해구·검단구·영종구·제물포구는 **실제 행정구역 개편**으로 생긴 구입니다. 어느 기존 구에서
갈라졌는지 매핑할 근거가 데이터에 없어 **통합하지 않고**, 인천의 다른 구가 전부 `인천○구` 형태인 것에
맞춰 접두어만 붙였습니다 → `인천서해구`(35) `인천검단구`(22) `인천영종구`(9) `인천제물포구`(7).

### B-3. 세종 도로명 19행 → `세종시`로
세종은 단층제라 자치구가 없습니다. `나성로`·`보듬3로` 같은 **도로명만** `세종시`로 돌렸고,
**조치원읍·연서면·금남면·장군면 같은 실제 읍·면은 그대로 뒀습니다**(행정 단위라 정보 손실이 없음).
도로명은 이미 주소 컬럼에 들어 있어 잃는 게 없습니다.

### B-4. 이름만 있고 지역이 빈 3행 → 보존 결정
확인해보니 "완전히 빈 행"이 아니라 **이름은 멀쩡한데 지역만 비어 있는** 행이었습니다
(나중환신경외과의원, 평화연합신경외과의원, 학교법인연세대학교치과대학치과병원).
장비가 붙어 있어 지우면 고아가 되고, 이름으로 검색은 되며 좌표가 없어 지도에는 안 뜹니다. **그대로 둡니다.**

### B-5. 주소·좌표 잔여 누락 → 현 상태 유지
- 주소 없음 4,111행 중 **3,507행은 요양기호 자체가 없어** 심평원에 존재하지 않는 기관입니다(주로 동물병원).
- **주소는 있는데 좌표가 없는 219행**은 지오코딩으로 채울 수 있지만, 서버용 지오코딩 키가 없습니다.
  (현재 있는 네이버 키는 지도 표시용 JS 키라 서버 주소변환이 안 됩니다)
  → **NCP 지오코딩 키를 주시면 219행을 채울 수 있습니다.**

### B-6. 커뮤니티 글 → 삭제 완료
글 1건 + 댓글 1건 삭제. 유일한 글이 "HS70A 초기화 방법"이라 기술 자료일 수 있어
삭제 전에 `backend/deleted_community_posts_backup.json`으로 백업해 뒀습니다
(확인 결과 **본문이 비어 있어** 실제로 잃은 내용은 없습니다).

---

## C. 추가 데이터 관련 — 결정 반영됨

### C-1. 폐업·휴업 현황 — 여전히 불가
심평원 병원정보서비스에 **폐업일자가 없습니다.** 화면에 "제공하지 않음"으로 명시해 두었습니다.

### C-2. 인구 1만명당 지표 — ❌ 불필요 결정
지시대로 추진하지 않습니다. 분포 화면에 있던 "인구 데이터가 없어 제공하지 않습니다" 안내 문구도 제거했습니다.

### C-3. 지역 × 진료과 히트맵 — ❌ 불필요 결정
지시대로 추진하지 않습니다. 현재의 지역 × 종별 집계를 유지합니다.

### C-4. 심평원 전체 장비군 — ✅ 진행
원본 CSV(`건강보험심사평가원_의료장비 상세 현황_20251231`)를 확인한 결과
**장비대분류 195종 / 711,808행**입니다(저주파자극기 58,903, 초음파영상진단기 38,848,
적외선조사기 26,002, 광중합기 25,013, 치과용방사선촬영장치 23,274 …).

현재 DB의 510,169행은 **6종 × 7년치**(연도별 파일 유래)라 성격이 달라서, 중복 없이 합치는 작업을
별도로 진행 중입니다. 결과는 완료 후 추가합니다.

---

## D. 알아두시면 좋은 것 (조치 불필요)

- **직원가입은 자동승인 유지**입니다. 승인제로 바꾸시려면 서버 환경변수
  `SIGNUP_AUTO_APPROVE_STAFF=false` 후 백엔드 재기동.
- `계약서.jpg`는 **실제 고객 정보가 들어 있어 저장소에 커밋하지 않았습니다.**
- 사용하지 않는 템플릿 잔재 파일 몇 개(`tables/BasicTableOne.tsx`, `form/date-picker.tsx` 등)가
  있습니다. 어디서도 불러쓰지 않아 건드리지 않았고 삭제도 하지 않았습니다.
- 서버 디스크가 가득 차서 배포가 한 번 실패한 적이 있는데, 백업 폴더가 도커 이미지로
  복사되던 게 원인이었습니다. 수정 완료(여유 4.8G → 6.9G).
