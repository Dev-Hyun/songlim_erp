# songlim-medical.com 도메인 연결 가이드

가비아에서 구매한 **songlim-medical.com** (3년)을 현재 NCP 서버에서 운영 중인 ERP에 연결하는 방법입니다.
지금은 `http://<서버IP>:3000` 형태(IP + HTTP)로 접속하고 있고, 이 작업이 끝나면
**https://songlim-medical.com** 하나로 접속하게 됩니다.

> 이메일(songlim-medical.com 메일 40명)은 요청대로 **이번엔 구현하지 않습니다.** 문서 맨 아래에
> 나중에 붙일 때의 방향만 짧게 남겨둡니다.

---

## 전체 그림

```
사용자 ──(https:443)──►  Caddy(리버스 프록시, 서버에 새로 추가)
                          ├─ /            → frontend 컨테이너(:3000)
                          └─ /api/*       → backend 컨테이너(:8010)
```

- **한 도메인(songlim-medical.com)** 아래에서 화면과 API를 모두 제공 → CORS 문제 없음, 쿠키도 안전.
- HTTPS 인증서는 **Caddy가 Let's Encrypt로 자동 발급/자동 갱신**합니다(수동 certbot 불필요).

---

## 1단계. 가비아 DNS 설정 (도메인 → 서버 IP 연결)

1. 서버 공인 IP 확인: NCP 콘솔의 서버(VM) 상세, 또는 서버에서 `curl ifconfig.me`.
2. 가비아 로그인 → **My가비아 → 도메인 → songlim-medical.com → DNS 관리(DNS 설정)**.
3. 아래 레코드를 추가/수정합니다. (`<서버IP>`를 실제 IP로 교체)

   | 타입 | 호스트 | 값/위치 | TTL |
   |---|---|---|---|
   | A | @ | `<서버IP>` | 600 |
   | A | www | `<서버IP>` | 600 |

4. 저장. DNS 전파는 보통 10분~1시간(최대 몇 시간) 걸립니다.
   - 확인: `nslookup songlim-medical.com` 결과가 `<서버IP>`로 나오면 완료.

---

## 2단계. 서버 방화벽/포트 열기

- NCP **ACG(방화벽)** 에서 인바운드 **80(TCP), 443(TCP)** 를 허용합니다.
- 기존 3000/8010은 굳이 외부에 열어둘 필요가 없어집니다(리버스 프록시 뒤로 숨김). 보안상 닫는 걸 권장.

---

## 3단계. 리버스 프록시(Caddy) 추가

서버의 `/opt/songlim_erp`(이 저장소가 clone된 위치)에서 진행합니다.

### 3-1. `Caddyfile` 생성 (`/opt/songlim_erp/Caddyfile`)

```
songlim-medical.com, www.songlim-medical.com {
    encode gzip

    # API는 백엔드로
    handle /api/* {
        reverse_proxy backend:8010
    }

    # 나머지는 프론트엔드로
    handle {
        reverse_proxy frontend:3000
    }
}
```

### 3-2. `docker-compose.yml`에 caddy 서비스 추가

기존 `services:` 아래에 아래 블록을 추가합니다(기존 backend/frontend는 그대로 두되, 외부 포트
매핑 `ports:`는 지워도 됨 — 이제 caddy를 통해서만 들어옴).

```yaml
  caddy:
    image: caddy:2
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - backend

volumes:
  caddy_data:
  caddy_config:
```

> `caddy_data`에 발급받은 인증서가 저장되므로 컨테이너를 새로 만들어도 인증서가 유지됩니다.

---

## 4단계. 환경변수 변경 (도메인 기준으로)

프론트엔드는 **빌드 시점**에 API 주소가 박히므로(Next.js 특성) 반드시 build arg로 새 값을 넘겨
**다시 빌드**해야 합니다.

`/opt/songlim_erp/.env` (docker compose가 읽는 환경파일)에서:

```bash
# 프론트가 호출할 API 베이스 — 같은 도메인이므로 https 도메인만
NEXT_PUBLIC_API_URL=https://songlim-medical.com

# 백엔드 CORS 허용 오리진(같은 도메인)
CORS_ORIGINS=https://songlim-medical.com,https://www.songlim-medical.com

# HTTPS가 붙었으므로 세션 쿠키 Secure 활성화 (이걸 켜야 로그인 유지됨)
COOKIE_SECURE=true
```

- `backend/app/main.py`의 CORS는 `CORS_ORIGINS`를, 세션 쿠키는 `COOKIE_SECURE`를 이미 환경변수로
  읽습니다. `NEXT_PUBLIC_API_URL`은 `docker-compose.yml`의 frontend build args로 전달됩니다.

---

## 5단계. 배포(재빌드) 및 확인

```bash
cd /opt/songlim_erp
git pull origin main
docker compose up -d --build          # frontend 재빌드로 새 API 주소 반영 + caddy 신규 기동
docker compose logs -f caddy          # 인증서 자동 발급 로그 확인 (Ctrl+C로 빠져나오기)
```

확인:
- 브라우저에서 **https://songlim-medical.com** 접속 → 자물쇠(🔒) 표시 + 정상 로그인.
- `http://songlim-medical.com` 접속 시 자동으로 https로 리다이렉트되는지.
- 로그인 후 새로고침해도 세션이 유지되는지(COOKIE_SECURE 정상 동작 확인).

### 자주 막히는 부분
- **인증서 발급 실패**: DNS 전파(1단계)가 끝나기 전이거나 80/443 포트(2단계)가 막혀 있으면 Let's
  Encrypt 검증이 실패합니다. `nslookup`으로 IP 확인 + ACG 포트 확인 후 `docker compose restart caddy`.
- **로그인이 안 됨(로그인 직후 튕김)**: `COOKIE_SECURE=true`인데 http로 접속하면 쿠키가 저장되지
  않습니다. 반드시 https로 접속.
- **화면은 뜨는데 데이터가 안 옴**: 프론트를 `--build` 없이 올려서 옛 `NEXT_PUBLIC_API_URL`(예전 IP)이
  그대로 박혀 있는 경우. `docker compose up -d --build frontend`로 다시 빌드.

---

## (참고) 이메일은 나중에 붙일 때

이번엔 구현하지 않지만, 방향만:
- **받는 것 포함 자체 메일서버 운영은 비권장**(스팸 처리/보안/운영부담 큼). 40명 규모면
  **가비아 웹메일/구글 워크스페이스/네이버웍스** 같은 메일 호스팅을 도메인에 붙이는 방식이 현실적입니다.
  → 이 경우 가비아 DNS에 **MX 레코드**(메일 수신 서버)와 **SPF/DKIM**(발신 인증) TXT 레코드를 추가합니다.
- **사이트에서 발송만** 필요하면(계약/발주 알림 메일 등) 위 메일 계정의 **SMTP 정보**로 백엔드에서
  발송하는 기능만 추가하면 됩니다(수신은 웹메일에서). 필요해지면 이때 붙이겠습니다.
```
