# ── 시크릿 ──
.env
*.env
!.env.example

# ── DB / 백업 (배포는 볼륨 마운트+백업으로 관리, git으로 관리하지 않음) ──
backend/data.db
backend/data.db-shm
backend/data.db-wal
backend/data.db.bak_*
backend/uploads/
backend/backups/

# ── 로그 ──
*.log

# ── Python ──
__pycache__/
*.py[cod]
venv/
env/
.venv/
*.egg-info/

# ── Node / Next.js ──
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# ── IDE / OS ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 로컬 전용 개발 기록 (원격에 push하지 않음) ──
/PROGRESS.md
/New.md
/0725review.md
/.claude/

# ── 참고용 엑셀 자료 (외부 출처 데이터 — 카탈로그 자동생성에 쓰지 않고 로컬 참고용으로만 보관) ──
*.xlsx

# ── NCP 인증키/인프라 메모 (절대 원격에 올리지 않음) ──
/NCP/

# ── GitHub Wiki에 게시할 문서 초안 (실제 게시물은 별도 wiki 저장소, 여긴 로컬 초안 스테이징용) ──
/docs_wiki_draft/

# local dev DBs / stray artifacts
backend/app.db
backend/data.db.bak*
image.png

# 데이터 정리 스크립트가 만드는 되돌리기/보고 산출물 — 실행한 머신의 행 ID라 공유하면 위험하다
backend/*_undo.json
backend/deleted_community_posts_backup.json
backend/scripts/coord_outliers.json
# 실제 고객 정보가 담긴 표본 문서
계약서.jpg
영업지도_전수조사_결과.md

# 분석·정리·가이드 문서는 보고서/ 안에 모아두고 저장소에는 올리지 않는다 (사용자 지시 2026-09-19).
# 실제 고객 정보와 내부 운영 메모가 섞여 있어 공유 저장소에 남기면 안 된다.
보고서/
# 심평원 원본 장비 데이터 (수백 MB). 저장소에도 도커 이미지에도 들어가면 안 된다.
의료장비 상세현황/
# 심평원/식약처 원본 수신 캐시 (143MB). 재실행하면 다시 받는 파생 데이터라 저장소에 둘 이유가 없다.
backend/scripts/_audit_cache/
